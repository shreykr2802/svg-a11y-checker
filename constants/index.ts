const DefaultConfig = {
  ignorePatterns: ["node_modules/**", "build/**"],
  rules: {
    requireTitle: true,
    requireDescription: true,
    checkContrast: true,
    checkAriaLabel: true,
  },
};

export { DefaultConfig };
